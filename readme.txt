Use the command below install next globally:

npm i -g next


Usage:

To reset user cache:

- Update cache prefix in the ITA Authentication ID record
- Use the following API call to reset user cache on the portal
/api/cache/resetusercacheprefix?email=ashlega@gmail.com&admin_key=<ADMIN_KEY>
- Note: portal admin key is to be added to the env.local